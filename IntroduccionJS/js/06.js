// Objeto Math

let resultado;

resultado = Math.PI; // el pi de matemaricas
resultado = Math.round(2.5); // Redondea a un numero
resultado = Math.ceil(2.1); // cell siempre redondea hacia arriba
resultado = Math.floor(2.9); // cell siempre redondea hacia abajo
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-200); // convierte a positivo
resultado = Math.min(1,9,7,3,5,4,2,1,5,6,9); // Numero de menor valor
resultado = Math.max(1,9,7,3,5,4,2,1,5,6,9); // Numero de mayor valor
resultado = Math.random(); // Devulve un numero aleatorio
resultado = Math.round(Math.random()*10); // Numero aleatorio redondedado por 10

console.log(resultado);